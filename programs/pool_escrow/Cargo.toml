[package]
name = "pool_escrow"
version = "0.1.0"
edition = "2021"
description = "Escrow de partidas 1v1 de 8-Ball em SOL nativo"

[lib]
crate-type = ["cdylib", "lib"]
name = "pool_escrow"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.31.1"
