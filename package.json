{
  "name": "zinc-pool",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run dev:server & bun run dev:web",
    "dev:web": "bun run --cwd apps/web dev",
    "dev:server": "bun run --cwd apps/server dev",
    "build": "bun run --cwd apps/web build",
    "preview": "bun run --cwd apps/web preview",
    "typecheck": "bunx tsc --noEmit -p packages/engine-physics && bunx tsc --noEmit -p packages/engine-rules && bunx tsc --noEmit -p packages/replay && bunx tsc --noEmit -p apps/web && bunx tsc --noEmit -p apps/server && bunx tsc --noEmit -p scripts",
    "test": "bun run --cwd packages/engine-physics test && bun run --cwd packages/engine-rules test && bun run --cwd packages/replay test && bun run --cwd apps/server test && bun run --cwd apps/web test",
    "verify": "bun scripts/verify.ts",
    "cycle": "bun scripts/devnet-settle.ts",
    "replay": "bun scripts/devnet-replay.ts"
  },
  "devDependencies": {
    "@noble/hashes": "^2.3.0",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@zinc-pool/chain-client": "workspace:*",
    "@solana/web3.js": "^1.98.0",
    "@zinc-pool/replay": "workspace:*",
    "@zinc-pool/engine-physics": "workspace:*"
  }
}
